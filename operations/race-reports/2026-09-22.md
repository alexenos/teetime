# Race report: 2026-09-22

**Target:** Tuesday 2026-09-29, Northgate — 08:38 AM, 4 players, one racer task
(`b1e6357f`).
**Window:** Tuesday 2026-09-22 06:30:00 CT (job fires 06:28; batch started
06:28:00.063, pre-locate/staging at 06:28:22.399).
**Outcome: won**, on a fallback. 08:38 AM was never a bookable row this
morning — it sits inside a block the observer reads as `08:26 AM–10:42 AM,
event, never Empty` across all nine snapshots. The pre-window slot scan
excluded it from candidates for the same reason. The bot pre-located its
nearest real fallback, 08:18 AM, and won it on the very first Reserve of the
opening burst at **+1027ms**, dead on the sweep's +1030ms aim point.
Confirmed by `RESERVATION_CHECK` against the member's own reservations page,
not just the "thank you" phrase match.
**Code that ran:** `e7d89ac` (#217), merged 2026-09-19 18:59 CT — includes the
halved opening burst from #215 (burst of 6, matching this morning's log).
#221 (the `booking-postmortem` → `race-report` skill rename, and the
`docs/` → `operations/` move this very file benefits from) merged at
06:40:43 CT, ten minutes after this race, so it had no effect on the run
being reported here.

**Status: this morning is closed.** Clean win on the intended fallback, fully
corroborated by the observer.

---

## This morning, in order

| Stage | Detail |
|---|---|
| Login → sheet | Steps 1-4 clean, 06:28:06.157 → 06:28:22.281 |
| Slot scan | 126 rows → 2 candidates for 08:38 AM ±32min (dropped capacity=1, course=64, window=59) |
| Pre-locate | 08:38 AM not found; fell back to `index=6`, 08:18 AM, `exact=False`, `available=4`, 1 fallback behind it (08:10 AM) |
| Clock skew | club −10ms from us, one-way 14ms (106 probes, 5 transitions, tick ±24ms) |
| Lead | Reserve sent 3ms early |
| Opening burst | 6 planned, all target-only (08:18 AM), no fallback staged |
| Fire (winning rung) | attempt **1** (burst member #0), **+1027ms** past the window |
| Answer | **accepted** — club clock 1000ms past (club `:01`), round trip **614ms**, 86149 bytes, `form=08:18 AM`, `popup=False` |
| Other attempts | #2 refused @1127ms (RT 735ms) · #3 refused @1247ms (RT 427ms) · #4 accepted @1440ms (RT 329ms, surplus) · #5 accepted @1547ms (RT 327ms, surplus) |
| `GATE:` | club `:01` asked (5), **GRANTED** — all 5 asks and both refusals landed in the same club second as the grant |
| Ledger | `club granted 08:18 AM at +1027ms past the window; last refusal was +1247ms` |
| Chain | player count → 3× TBD guest → Book Now, 78–543ms per step |
| Chain finished | `phase=complete, success=True, blocked=False, attempts=5, totalMs=2009` |
| Verify | `RESERVATION_CHECK: Reservation found - TEE TIMES (NORTHGATE) 09/29/2026 08:18 AM - 08:26 AM RESERVED` |

The winning response carried the accept `<eval>` signature, no `.show()`:

```javascript
executeHoldTimeTimer('300');;stopSheetTimers();;scrollToElement('.tee-time-flag', 50);;
```

Both refusals carried `PF('teeSheetValidationErrorPopupVar').show();;` — separated
cleanly by the `<eval>`, per §5 of the skill, not by the popup markup (which
the grant's own response also echoed once, per its own log line: *"The
granting response also carried 'Slot blocked by another user'; treating it
as stale for the rest of the chain"* — expected, not a fault).

Artifacts:
`gs://gen-lang-client-0822973627-teetime-debug-artifacts/walden/race/20260922_113003_teetime-racer-kfjvp_0/`
(5 ledger rows, 5 raw payloads stored).

### Three grants for one slot, not two

The club accepted 08:18 AM three separate times in this burst: burst member
#0 (the win, attempt 1), and members #3 and #4 (attempts 4 and 5) as surplus
holds after the slot was already ours. The existing code comment frames the
open question as "whether the club tolerates two" surplus grants; this
morning produced **three** concurrent holds on the same slot (one real, two
surplus), which the code already treats as expected and leaves to the club's
own hold timer to expire. Nothing needed fixing today, but it's a data point
worth folding into whatever eventually characterizes that behavior (see
Recommendations).

### The observer corroborates both halves of the story

Per §7f this is a standing check, run whether or not the boundary is in
question. The observer (`20260922_112657_teetime-observer-255cq`, 814
Northgate row ids, a real read) shows:

| course | idx | slot | state | flip / notes |
|---|---|---|---|---|
| Northgate | 6 | 08:18 AM (booked) | empty → disabled | `(+705, +1641]ms` — brackets the +1027ms grant |
| Northgate | 7 | 08:26 AM–10:42 AM (contains 08:38 AM target) | `event` | never `Empty` at any of the 9 snapshots, −500ms through +7790ms |

Row 7 is the independent confirmation that 08:38 AM was never open this
morning — not lost in the race, never contestable in it at all.

### Round trip: comfortably inside budget

Round trips this morning (614, 735, 427, 329, 327ms) are all well under both
active timeouts (`_RESERVE_OPENING_TIMEOUT_S` = 10.0s for the burst,
`_RESERVE_TIMEOUT_S` = 3.0s for the serial fallback walk, unused today). No
timeout risk, nothing near either figure.

---

## Established vs hypothesis

**Established**

- 08:38 AM was never bookable this morning: excluded from the pre-window
  slot scan's candidates, and the observer shows its containing block as
  `event`/never-Empty across all nine snapshots.
- The fallback, 08:18 AM, was won cleanly on the first Reserve of the burst
  at +1027ms — within 3ms of the sweep's +1030ms aim.
- The win is a real reservation, not a phrase-match false positive —
  `RESERVATION_CHECK` found it by name on the member's own reservations page.
- The observer's flip table for 08:18 AM (`empty` → `disabled` in
  `(+705, +1641]ms`) brackets and corroborates the ledger's +1027ms grant.
- Round trips were nowhere near either active Reserve timeout.
- The code that ran was `e7d89ac` (#217); the same-morning skill rename
  (#221) merged after the race and did not affect this run.

**Hypothesis**

- None needed. Nothing about this morning's outcome or its evidence is in
  question.

**Cheapest experiment that would discriminate:** none needed; there is
nothing left unresolved to discriminate between.

## Recommendations

1. **No change to the race path.** Clean win on the first rung, well inside
   every active timeout budget.
2. **Worth a watch, not a fix:** today produced three concurrent grants for
   one slot (one real, two surplus) rather than the two the existing surplus-
   hold comment anticipates. Fold this into whichever future ad-hoc test
   ends up characterizing how many surplus holds the club tolerates; no
   action needed from this morning alone.
3. **No IAM or infrastructure follow-up.** Setup, log pull, and artifact
   fetch all worked cleanly on the existing storage + logging grant.
