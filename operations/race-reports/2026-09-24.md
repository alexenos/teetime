# Race report: 2026-09-24

**Target:** Thursday 2026-10-01, Northgate — 08:08 AM, 4 players, one racer task
(`11161cb0`).
**Window:** Thursday 2026-09-24 06:30:00 CT (job fires 06:28; batch started
06:28:00.056, pre-locate/staging at 06:28:20.848).
**Outcome: won**, clean, on the very first Reserve of the opening burst.
08:08 AM pre-located exactly (`index=7, exact=True`), fired at **+1010ms**,
accepted immediately — dead on the sweep's +1030ms aim point. Confirmed by
`RESERVATION_CHECK` against the member's own reservations page, not just the
"thank you" phrase match. Independently corroborated by the observer's flip
table for the same slot.
**Code that ran:** `1e7ccbe` (#223), merged 2026-09-22 14:20 CT. That PR only
touches `.coderabbit.yaml` (skip CodeRabbit review on race reports); no code
change has landed since #217 (2026-09-19, the halved opening burst), so this
morning ran the same race path as 09-22.

**Status: this morning is closed.** Clean win, fully corroborated, no issues
found.

---

## This morning, in order

| Stage | Detail |
|---|---|
| Login → sheet | Steps 1-4 clean, 06:28:05.159 → 06:28:20.731 CT |
| Slot scan | 151 rows → 9 candidates for 08:08 AM ±32min (dropped course=64, window=78) |
| Pre-locate | 08:08 AM found exactly, `index=7, exact=True, available=4`, 8 fallbacks behind it (7 off-grid) |
| Aim | target timestamp 06:30:00.000 CT +1030ms (club opens at +1000ms, 30ms margin) |
| Clock skew | club +8ms from us, one-way 12ms (114 probes, 5 transitions, tick pinned ±22ms) |
| Lead | Reserve sent 20ms early so it arrives as the window opens |
| Opening burst | 6 planned, all target-only (08:08 AM), 0 fallbacks staged in the burst |
| Fire (winning rung) | attempt **1** (burst member #0), **+1010ms** past the window |
| Answer | **accepted** — club clock 1001ms past (club `:01`), round trip **696ms**, 86150 bytes, `form=08:08 AM`, `popup=False` |
| Other attempts | #2 refused @1110ms (RT 465ms) · #3 refused @1230ms (RT 415ms, `frozen=True`) · #4 refused @1380ms (RT 581ms) · #5 accepted @1530ms (RT 248ms, surplus) · #6 accepted @1710ms (RT 256ms, surplus) |
| `GATE:` | club `:01` asked (6), **GRANTED** — all 6 asks and 3 refusals landed in the same club second as the grant |
| Ledger | `club granted 08:08 AM at +1010ms past the window; last refusal was +1380ms` |
| Chain | player count → 3× TBD guest → Book Now, 79–1233ms per step |
| Chain finished | `phase=complete, success=True, blocked=False, attempts=6, totalMs=3159` |
| Verify | `RESERVATION_CHECK: Reservation found - TEE TIMES (NORTHGATE) 10/01/2026 08:08 AM - 08:15 AM RESERVED` |

The winning response carried the accept `<eval>` signature, no `.show()`:

```javascript
executeHoldTimeTimer('300');;stopSheetTimers();;scrollToElement('.tee-time-flag', 50);;
```

All three refusals carried `PF('teeSheetValidationErrorPopupVar').show();;` —
separated cleanly by the `<eval>`, per §5 of the skill, not by the popup
markup (which the grant's own response also echoed once, per its own log
line: *"The granting response also carried 'Slot blocked by another user'
(blocked by another user)'; treating it as stale for the rest of the
chain"* — expected, not a fault).

Artifacts:
`gs://gen-lang-client-0822973627-teetime-debug-artifacts/walden/race/20260924_113004_teetime-racer-ndpzj_1/`
(6 ledger rows, 6 raw payloads stored).

### Two surplus grants, same pattern as before

Burst members #4 and #5 were also granted 08:08 AM after member #0 already
won it — two surplus holds, left to the club's own hold timer to expire, the
same pattern seen before (e.g. 09-22's three concurrent grants). Nothing
needed fixing today.

### The observer corroborates the grant

Per §7f this is a standing check, run whether or not the boundary is in
question. The observer (`20260924_112545_teetime-observer-n5c5v`, 1134
Northgate row ids, a real read, watching 2026-10-01) shows:

| course | idx | slot | state | flip / notes |
|---|---|---|---|---|
| Northgate | 7 | 08:08 AM (booked) | empty → disabled | `(+618, +1566]ms` — brackets the +1010ms grant |

The interval containing the flip (`+618ms` to `+1566ms`) brackets the
ledger's +1010ms grant exactly, independent corroboration that the slot was
open at the window and taken in this race, not pre-held. (The flip's
resulting state reads `disabled` with `0 named + 0 TBD` at the time of the
last snapshot, +7506ms — before the chain's player/guest additions had
propagated back to the sheet's datascroller; `RESERVATION_CHECK` against the
member's own reservations page, not the sheet's row rendering, is the
authoritative confirmation, and it found the reservation by name.)

### Round trip: comfortably inside budget

Round trips this morning (696, 465, 415, 581, 248, 256ms) are all well under
both active timeouts (`_RESERVE_OPENING_TIMEOUT_S` = 10.0s for the burst,
`_RESERVE_TIMEOUT_S` = 3.0s for the serial fallback walk, unused today — the
target was won inside the opening burst). No timeout risk, nothing near
either figure, no new high-water mark on the roundTripMs table in §7b of the
skill.

---

## Established vs hypothesis

**Established**

- 08:08 AM was won cleanly on the first Reserve of the opening burst at
  +1010ms — within 20ms of the sweep's +1030ms aim.
- The win is a real reservation, not a phrase-match false positive —
  `RESERVATION_CHECK` found it by name on the member's own reservations page.
- The observer's flip table for 08:08 AM (`empty` → `disabled` in
  `(+618, +1566]ms`) brackets and corroborates the ledger's +1010ms grant.
- Round trips were nowhere near either active Reserve timeout; no new
  high-water mark.
- The code that ran was `1e7ccbe` (#223), a docs-only CodeRabbit-config
  change on top of #217 — the race path itself is unchanged since 09-19.

**Hypothesis**

- None needed. Nothing about this morning's outcome or its evidence is in
  question.

**Cheapest experiment that would discriminate:** none needed; there is
nothing left unresolved to discriminate between.

## Recommendations

1. **No change to the race path.** Clean win on the first rung, well inside
   every active timeout budget.
2. **No new fix identified.** This morning surfaced nothing to escalate.
3. **No IAM or infrastructure follow-up.** Setup, log pull, and artifact
   fetch all worked cleanly on the existing storage + logging grant.
